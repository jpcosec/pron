# Revision lock: the commits of pron's sibling dependencies this checkout is verified
# against. pron installs them editable from sibling checkouts (sldb, kgdb), so a sibling
# moving under pron silently changes pron's test floor — pin here, verify here.
#
# Verified against (suites), 2026-09-17, pron <A4-sha> (fusion-kgdb: sldb.api.edges replaces
# .pron/graph.nx.json + kgdb; EdgeReader has one door):
#   - sldb fusion-kgdb ffd7d19 (models: RelationDoc/RelationTypeDoc to sldb.models; store:
#     edge index as per-document shards, synced on every write; api: sldb.api.edges +
#     `sldb edges`), 4 commits on f48a154, 744 passed, 5 failed (the floor's 5, none new);
#   - kgdb fusion-kgdb 81e81bc (kgdb.models reexports from sldb; init_world delegates to
#     sldb.api.init_relations; parity test), 2 commits on 66f6c0e, 57 passed;
#   pron: <N> passed, <M> snapshots (see briefs/A4.RESULT.md for the golden diff). No more
#   kgdb or networkx dependency: pron reads sldb.api.load_edge_index directly.
#
# Verified against (suites), 2026-09-17, pron 591a86f (sldb.api instead of sldb.cli classes):
#   - kgdb 5effed5; sldb 23f78c5 (api: typed sldb.api library package; the CLI uses it): 195 passed.
#
# Verified against (suites), 2026-09-15, pron 052bdb4 (+corpus c534d77, README compuesto, serve settle):
#   - kgdb 5effed5; sldb 60943c7 (where: escaped literals): 158 passed.
#
# Verified against (suites), 2026-09-15, pron fa66935 (move resolves its pending creates; where errors):
#   - kgdb 5effed5; sldb a508034 (where: empty literal, unparseable predicate is an error): 137 passed.
#
# Verified against (suites), 2026-09-15, pron a2d8f53 (merkle capa 8: light refresh after an own write):
#   - kgdb 5effed5; sldb 8699177 (main, merkle capa 8): 129 passed.
#
# Verified against (suites), 2026-09-15, pron 742cc29:
#   - kgdb 5effed5; sldb 00b2dd4 (main, merge 'merkle': per-document shards for semantic,
#     sections and documents index; a write touches only its own path): 129 passed.
#
# Verified against (suites), 2026-09-15, pron b04f88d (merge 'merkle': refresh passes the previous
# graph to kgdb's incremental build_typed_snapshot(previous=), hash_mundo memoized per turn):
#   - kgdb 5effed5; sldb working tree (b0f196a + uncommitted changes of another session): 129 passed.
#   - kgdb 5effed5; sldb merkle 6c6b173 (branch `merkle` in the sldb repo, not on main: its section
#     rebuild overlaps uncommitted work there): 128 passed, 1 failed (the same promote failure below).
#   pron's refresh requires kgdb >= 5effed5 (the `previous=` argument).
#
# Verified against (suites), 2026-09-15, pron ae2d0a3 + test fix:
#   - kgdb 846f331; sldb working tree (b0f196a + uncommitted changes of another session): 129 passed.
#   - kgdb 846f331; sldb b0f196a clean: 128 passed, 1 failed
#     (test_13_model_editing promote: `models validate --promote --format json` prints non-JSON
#     without the uncommitted models_validate.py/models_validate_utils.py fix). Pin sldb to the
#     commit that lands that fix.
# Earlier floor: sldb 9ba886d, kgdb 1247139 — 98 passed.

sldb ffd7d19 api: sldb.api.edges — leer aristas y nodos, rebuild, check e init_relations; `sldb edges` lo usa
kgdb 81e81bc tests: paridad edge_index vs typed_ingest (kgdb.models reexporta desde sldb)

"""Level 3 of the MCP surface (spec 14 §4, nivel 3; §5): a model generated from a JSON
declaration as a `StructuredNLDoc` module and registered in the world, and a registered
model edited through its draft. The only writes below the forms, so each one previews
without `confirm` and touches nothing.
"""

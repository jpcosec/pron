---
id: surface-pron-sexpr-forms-form_error
system: pron
surface: sexpr.forms.form_error
tags:
- system:pron
- domain:system_architecture
- kind:software
- impl:here
- entity:module
provenance: src/pron/sexpr/forms/form_error.py
---

# sexpr.forms.form_error

## Purpose

What a badly made form raises (spec 13). The two cases a caller distinguishes —
a bare noun where a move belongs, an unknown word — are subclasses of this one.

## How It Works

What a badly made form raises (spec 13). The two cases a caller distinguishes —
a bare noun where a move belongs, an unknown word — are subclasses of this one.

## Commands

FormError

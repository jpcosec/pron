---
id: surface-pron-sexpr-resolving-edge_read
system: pron
surface: sexpr.resolving.edge_read
tags:
- system:pron
- domain:system_architecture
- kind:software
- impl:here
- entity:module
provenance: src/pron/sexpr/resolving/edge_read.py
---

# sexpr.resolving.edge_read

## Purpose

The result of reading edges (spec 03): the edges themselves, whether they came from the
typed graph kgdb built or from the RelationDocs in sldb, and the exact queries it took, so
the trace can say which door answered.

## How It Works

The result of reading edges (spec 03): the edges themselves, whether they came from the
typed graph kgdb built or from the RelationDocs in sldb, and the exact queries it took, so
the trace can say which door answered.

## Commands

EdgeRead

---
id: surface-pron-kernel-add_verb
system: pron
surface: kernel.add_verb
tags:
- system:pron
- domain:system_architecture
- kind:software
- impl:here
- entity:module
provenance: src/pron/kernel/add_verb.py
---

# kernel.add_verb

## Purpose

The `add` verb (spec 11 §7): append a value to a list field, once, and remove it again
to undo.

## How It Works

The `add` verb (spec 11 §7): append a value to a list field, once, and remove it again
to undo.

## Commands

AddVerb

---
id: surface-pron-corpus-corpus_entry
system: pron
surface: corpus.corpus_entry
tags:
- system:pron
- domain:system_architecture
- kind:software
- impl:here
- entity:module
provenance: src/pron/corpus/corpus_entry.py
---

# corpus.corpus_entry

## Purpose

One document of the corpus (spec 12 §5b), identified as the world exports it: the export
id (`Model:doc`, `store:Model:doc`), never the bare name — two stores of a federated world
may hold the same document name.

## How It Works

One document of the corpus (spec 12 §5b), identified as the world exports it: the export
id (`Model:doc`, `store:Model:doc`), never the bare name — two stores of a federated world
may hold the same document name.

## Commands

CorpusEntry

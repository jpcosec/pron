---
id: surface-pron-kernel-actions-add_verb
system: pron
surface: kernel.actions.add_verb
tags:
- system:pron
- domain:system_architecture
- kind:software
- impl:here
- entity:module
provenance: src/pron/kernel/actions/add_verb.py
---

# kernel.actions.add_verb

## Purpose

The `add` verb (spec 11 §7): append a value to a list field, once, and remove it again
to undo.

## How It Works

The `add` verb (spec 11 §7): append a value to a list field, once, and remove it again
to undo.

## Commands

AddVerb

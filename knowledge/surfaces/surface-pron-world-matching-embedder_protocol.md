---
id: surface-pron-world-matching-embedder_protocol
system: pron
surface: world.matching.embedder_protocol
tags:
- system:pron
- domain:system_architecture
- kind:software
- impl:here
- entity:module
provenance: src/pron/world/matching/embedder_protocol.py
---

# world.matching.embedder_protocol

## Purpose

The approximate-matching port (spec 11 §2): an application injects an Embedder; without
one, pron falls back to difflib and says so in the trace. It never executes anything, it
only ranks neighbors to offer.

## How It Works

The approximate-matching port (spec 11 §2): an application injects an Embedder; without
one, pron falls back to difflib and says so in the trace. It never executes anything, it
only ranks neighbors to offer.

## Commands

Embedder

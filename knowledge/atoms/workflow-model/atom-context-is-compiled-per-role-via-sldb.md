---
id: atom-context-is-compiled-per-role-via-sldb
title: Context is compiled per role via SLDB
five_wh_one_plus: how
tags:
- system:deskops
---
# Context is compiled per role via SLDB
## Answer
Instead of free text reads, `ContextCompiler` queries the SLDB store to dynamically build a structured bundle targeting a specific role and task, guaranteeing non-negotiable guardrails via code.
